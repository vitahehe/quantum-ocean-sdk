The detailed description of the code and my final thesis can be found in the BTR_Vita_Stefanija document

## Setup

This project uses a Python virtual environment located at `.venv`. To install deps:

```bash
python -m venv .venv        # create venv if not present
source .venv/bin/activate   # activate environment
pip install -r requirements.txt
```

